# ✅ GPU-enabled PyTorch (CUDA 11.8)
torch==2.1.2+cu118
torchvision==0.16.2+cu118
torchaudio==2.1.2+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# === Core Scientific Libraries ===
numpy==1.24.4
scipy
scikit-image
pandas
SimpleITK
nibabel

# === CLI & Progress ===
tqdm>=4.64
argparse ; python_version < "3.10"  # Python 3.10+ 已内置 argparse

# === nnUNet v2 Core (inference only) ===
nnunetv2

# === Optional: HDF5 (if .h5 read/write is used) ===
h5py>=3.7

# === Optional: Visualization or Debugging ===
matplotlib>=3.5  # 可选，用于调试图像

# === Others ===
psutil        # 可选，用于 CPU 获取等性能控制
numba
openpyxl

# ✅ nnUNetv2 dependencies
monai
acvl-utils
blosc2
gdown
